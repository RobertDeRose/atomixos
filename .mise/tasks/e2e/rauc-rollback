#!/usr/bin/env bash
#MISE description="Run RAUC rollback E2E test"
#MISE alias="rollback"
set -euo pipefail

echo "Running E2E test: rauc-rollback"
echo "  Installs to slot B, marks B bad, verifies rollback to A"
echo ""
nix build .#checks.aarch64-linux.rauc-rollback --no-link --print-build-logs \
	2> >(grep -v 'SQLite database.*is busy' >&2)
echo ""
echo "PASS: rauc-rollback"
