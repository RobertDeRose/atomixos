#!/usr/bin/env bash
#MISE description="Run SSH-on-WAN toggle E2E test"
#MISE alias="ssh-toggle"
#USAGE flag "--lima" help="Run inside a Lima VM"
#USAGE flag "--vm <name>" help="Lima VM instance name" default="default"
set -euo pipefail
exec ./scripts/run-e2e-check.sh ssh-wan-toggle \
	"Verifies the flag file enables and disables SSH on WAN via nftables"
