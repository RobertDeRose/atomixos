#!/usr/bin/env bash
#MISE description="Run SSH-on-WAN toggle E2E test"
#MISE alias="ssh-toggle"
set -euo pipefail

echo "Running E2E test: ssh-wan-toggle"
echo "  Verifies flag file enables/disables SSH on WAN via nftables"
echo ""
nix build .#checks.aarch64-linux.ssh-wan-toggle --no-link --print-build-logs \
	2> >(grep -v 'SQLite database.*is busy' >&2)
echo ""
echo "PASS: ssh-wan-toggle"
