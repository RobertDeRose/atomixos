#!/usr/bin/env bash
#MISE description="Run RAUC update install E2E test"
#MISE alias="update"
#USAGE flag "--lima" help="Run inside a Lima VM"
#USAGE flag "--vm <name>" help="Lima VM instance name" default="default"
set -euo pipefail
exec ./scripts/run-e2e-check.sh rauc-update \
	"Builds and installs a test bundle, then verifies the slot switch A→B"
