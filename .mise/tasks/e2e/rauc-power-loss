#!/usr/bin/env bash
#MISE description="Run RAUC power-loss simulation E2E test"
#MISE alias="power-loss"
#USAGE flag "--lima" help="Run inside a Lima VM"
#USAGE flag "--vm <name>" help="Lima VM instance name" default="default"
set -euo pipefail
exec ./scripts/run-e2e-check.sh rauc-power-loss \
	"Crashes VM during RAUC install and verifies slot A is intact after reboot"
