<!DOCTYPE html>
<html lang="en" class="h-full bg-blue-100">
  <head>
    <title>{{ .MetaTitle }} - {{ .PageTitle }}</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="{{ .MetaDescription }}" />
    <meta name="author" content="{{ .MetaAuthor }}" />
    <script>
      (function () {
        var style = localStorage.getItem("shell:style") || "auto";
        var dark = style === "dark" || (style === "auto" && window.matchMedia && window.matchMedia("(prefers-color-scheme: dark)").matches);
        if (dark) document.documentElement.classList.add("pf-v6-theme-dark");
      })();
    </script>
    <link rel="shortcut icon" href="{{ pathjoin .ActionEndpoint "/assets/images/favicon.png" }}" type="image/png" />
    <link rel="icon" href="{{ pathjoin .ActionEndpoint "/assets/images/favicon.png" }}" type="image/png" />
    <link rel="stylesheet" href="{{ pathjoin .ActionEndpoint "/assets/google-webfonts/roboto.css" }}" />
    <link rel="stylesheet" href="{{ pathjoin .ActionEndpoint "/assets/line-awesome/line-awesome.css" }}" />
    <link rel="stylesheet" href="{{ pathjoin .ActionEndpoint "/assets/css/generic.css" }}" />
    {{ if eq .Data.ui_options.custom_css_required "yes" }}
      <link rel="stylesheet" href="{{ pathjoin .ActionEndpoint "/assets/css/custom.css" }}" />
    {{ end }}
  </head>

  <body class="h-full">
    <button type="button" class="atomixos-theme-toggle" aria-label="Toggle theme">
      <svg viewBox="0 0 16 16" aria-hidden="true"><path d="M10.8 13.7A6.3 6.3 0 0 1 7.6 1.4a5.1 5.1 0 1 0 6.9 6.9 6.3 6.3 0 0 1-3.7 5.4Z"/></svg>
    </button>
    <div class="app-page">
      <div class="app-content">
        <div class="app-container">
          <div class="logo-box">
            <img class="logo-img" src="{{ pathjoin .ActionEndpoint "/assets/images/atomixos-logo.png" }}" alt="AtomixOS" />
          </div>
          <div class="atomixos-auth-card atomixos-generic-card">
            <div class="app-gen-banner-box">
              <h1 class="app-gen-banner-header">{{ .PageTitle }}</h1>
              <p class="app-gen-banner-message">{{ .Data.message }}</p>
            </div>
            <div class="atomixos-generic-actions">
              {{ if .Data.go_back_url }}
                <a href="{{ .Data.go_back_url }}" class="atomixos-generic-action">{{ .Data.i18n_go_back_action }}</a>
              {{ end }}
              <a href="/auth/portal" class="atomixos-generic-action">Return to Portal</a>
            </div>
          </div>
        </div>
      </div>
    </div>
    <script src="{{ pathjoin .ActionEndpoint "/assets/js/generic.js" }}"></script>
    {{ if eq .Data.ui_options.custom_js_required "yes" }}
      <script src="{{ pathjoin .ActionEndpoint "/assets/js/custom.js" }}"></script>
    {{ end }}
  </body>
</html>
